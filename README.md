Sources for the plugin `plugin.batteryState`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.batteryState" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
